import 'package:flutter/material.dart';

class VipScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('VIP Access Plans')),
    );
  }
}